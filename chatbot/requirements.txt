# Core Web Framework (minimal dependencies)
fastapi>=0.109.0
uvicorn>=0.27.0  # Without [standard] to reduce dependencies
python-multipart>=0.0.6

# NOTE: These are already provided by OMD:
# - jinja2 (templating)
# - PyYAML (config parsing)

# Utilities
python-dotenv>=1.0.0

# Optional dependencies (install separately when needed):
# - For LLM integration: openai>=1.10.0 or anthropic>=0.18.0
# - For MCP integration: fastmcp>=0.2.0
# See requirements-optional.txt for full list
