# Core Web Framework (Python 3.11+)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# NOTE: These are already provided by OMD:
# - jinja2 (templating)
# - PyYAML (config parsing)

# Utilities
python-dotenv>=1.0.0
