[omd_servers]
# Add your OMD server hostnames or IPs here
# Examples:
# omd-prod ansible_host=192.168.1.100 ansible_user=root
# omd-dev ansible_host=omd-dev.example.com ansible_user=root
localhost ansible_connection=local

[omd_servers:vars]
# Default OMD version to install into
# omd_version=default

# Uncomment to use specific OMD version
# omd_version=5.30
